---
phase: 01-foundation-core-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml, src/main.rs, src/cli.rs, src/config/mod.rs, src/config/paths.rs, src/error.rs, src/utils/paths.rs]
autonomous: true

must_haves:
  truths:
    - "User can run activate binary without crashes"
    - "Config file is created on first run at ~/.config/activate/config.toml"
    - "Config file has default tracked directory and ignore patterns"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project dependencies and metadata"
      contains: "clap.*serde.*toml.*directories.*anyhow"
    - path: "src/config/mod.rs"
      provides: "Config struct with loading and defaults"
      exports: ["Config", "load_config"]
    - path: "src/cli.rs"
      provides: "CLI command structure with clap"
      exports: ["Cli", "Commands"]
  key_links:
    - from: "src/main.rs"
      to: "src/config/mod.rs"
      via: "use config::load_config"
      pattern: "config::load_config"
---

<objective>
Set up Rust project structure and implement configuration system with TOML file handling.

Purpose: Establish the foundation for the activate CLI tool with proper error handling and platform-appropriate configuration paths.
Output: Working Rust binary with configuration loading and CLI argument parsing ready for command implementation.
</objective>

<execution_context>
@/Users/tlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-crud/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Rust project with dependencies</name>
  <files>Cargo.toml, src/main.rs</files>
  <action>
    Create new Rust binary project and add all required dependencies from research.

    Initialize with: cargo init --name activate

    Add dependencies to Cargo.toml:
    - clap 4.5.60 with derive and env features for CLI parsing
    - rusqlite 0.38.0 with bundled and chrono features for database
    - serde 1.0.228 with derive for serialization
    - toml 1.0.3 for config files
    - directories 6.0.0 for platform paths
    - anyhow 1.0.100 for error handling
    - chrono 0.4.39 with serde for timestamps
    - path-clean 1.0.1 for path normalization
    - colored 3.1.1 for terminal colors
    - tabled 0.18.0 for table output
    - serde_json 1.0.137 for JSON formatting

    Create minimal src/main.rs that imports anyhow and returns Result<()>.
  </action>
  <verify>cargo build successfully compiles the project</verify>
  <done>Rust project initialized with all Phase 1 dependencies in Cargo.toml</done>
</task>

<task type="auto">
  <name>Task 2: Implement configuration system and CLI structure</name>
  <files>src/cli.rs, src/config/mod.rs, src/config/paths.rs, src/error.rs, src/utils/paths.rs</files>
  <action>
    Create the configuration management system and CLI command definitions.

    In src/cli.rs:
    - Define Cli struct with Parser derive
    - Define Commands enum with Add, Remove, List subcommands
    - Add proper clap attributes for help text
    - Define OutputFormat enum (Table, Json, Tsv)

    In src/config/mod.rs:
    - Create Config struct with tracked_directory and ignore_patterns
    - Implement Default trait with sensible defaults
    - Implement load_config() that creates config if missing
    - Use directories crate for ~/.config/activate/config.toml path
    - Handle parse errors gracefully, fall back to defaults

    In src/config/paths.rs:
    - Create get_config_dir() and get_data_dir() using directories crate
    - Ensure cross-platform compatibility (XDG on Linux, ~/Library on macOS, AppData on Windows)

    In src/utils/paths.rs:
    - Create canonicalize_project_path() function
    - Handle ~ expansion using directories crate
    - Use path_clean to normalize before canonicalization
    - Return proper anyhow errors with context

    In src/error.rs:
    - Define a simple error wrapper using anyhow
    - Set up proper error display formatting

    Update src/main.rs:
    - Parse CLI args with clap
    - Load config on startup
    - Create config directory if missing
    - Match on commands (leave implementations as todo!())
  </action>
  <verify>cargo run -- --help shows command structure, cargo run creates ~/.config/activate/config.toml</verify>
  <done>Configuration system loads/creates TOML config, CLI parses commands, path utilities handle canonicalization</done>
</task>

</tasks>

<verification>
1. Running `cargo build --release` produces a working binary
2. Running `activate --help` shows add/remove/list commands
3. First run creates ~/.config/activate/config.toml with defaults
4. Config file contains tracked_directory and ignore_patterns
</verification>

<success_criteria>
- Rust project compiles without warnings
- CLI shows proper help text with command descriptions
- Configuration file is created with platform-appropriate paths
- All error cases handled without panics
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-crud/01-01-SUMMARY.md`
</output>