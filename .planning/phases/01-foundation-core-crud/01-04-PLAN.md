---
phase: 01-foundation-core-crud
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified: [src/commands/list.rs, src/output/mod.rs, src/output/formatters.rs, src/main.rs]
autonomous: true

must_haves:
  truths:
    - "User can list all projects with activate list"
    - "List output shows projects in a formatted table"
    - "User can output as JSON with --format json"
  artifacts:
    - path: "src/commands/list.rs"
      provides: "List command implementation"
      exports: ["execute_list"]
    - path: "src/output/formatters.rs"
      provides: "Output formatting for different formats"
      exports: ["format_table", "format_json", "format_tsv"]
  key_links:
    - from: "src/commands/list.rs"
      to: "src/database/operations.rs"
      via: "list_projects function"
      pattern: "operations::list_projects"
---

<objective>
Implement list command with multiple output formats for viewing tracked projects.

Purpose: Allow users to see all their tracked projects in table, JSON, or TSV format.
Output: Working list command that retrieves and formats project data from the database.
</objective>

<execution_context>
@/Users/tlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-crud/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create output formatting module</name>
  <files>src/output/mod.rs, src/output/formatters.rs</files>
  <action>
    Build formatters for table, JSON, and TSV output.

    In src/output/mod.rs:
    - Export formatters module
    - Define OutputFormat enum if not in CLI (Table, Json, Tsv)

    In src/output/formatters.rs:
    - Create format_table(projects) using tabled crate:
      * Columns: Name, Path, State, Last Touched, Git Origin
      * Format timestamps as relative time (e.g., "2 days ago")
      * Use colored crate to color state (green=active, yellow=inactive, gray=archived)
      * Handle empty list with "No projects tracked" message
      * Use tabled::builder::Builder for clean table output

    - Create format_json(projects) using serde_json:
      * Serialize projects Vec to JSON
      * Pretty print with 2-space indentation
      * Include all fields from Project struct

    - Create format_tsv(projects):
      * Header row with tab-separated column names
      * One project per line, fields tab-separated
      * Escape tabs and newlines in field values
      * Unix-friendly format for piping to other tools

    Helper: Create format_timestamp(timestamp) to convert Unix timestamps to:
    - "just now" (< 1 minute)
    - "X minutes ago" (< 1 hour)
    - "X hours ago" (< 24 hours)
    - "X days ago" (< 30 days)
    - Full date for older
  </action>
  <verify>Unit tests for each formatter with sample data</verify>
  <done>Formatters produce correct output for table, JSON, and TSV formats</done>
</task>

<task type="auto">
  <name>Task 2: Implement list command and wire to main</name>
  <files>src/commands/list.rs, src/main.rs</files>
  <action>
    Create list command that uses formatters and connects to CLI.

    In src/commands/list.rs:
    - Create execute_list(db, state_filter, format) function
    - Call database::operations::list_projects with optional state filter
    - Validate state filter if provided (active/inactive/archived)
    - Match on format to call appropriate formatter:
      * OutputFormat::Table => output::formatters::format_table
      * OutputFormat::Json => output::formatters::format_json
      * OutputFormat::Tsv => output::formatters::format_tsv
    - Print formatted output to stdout
    - Return Ok(()) on success

    Update src/main.rs:
    - Import output module
    - Update Commands::List match arm:
      * Extract state and format from parsed args
      * Call commands::list::execute_list(&db, state.as_deref(), format)
    - Ensure list command works with empty database

    Update src/commands/mod.rs:
    - Export list module

    Edge cases:
    - Empty project list shows helpful message in table format
    - Invalid state filter shows error with valid options
    - JSON format works even with empty list ([])
  </action>
  <verify>cargo run -- list, cargo run -- list --format json, cargo run -- list --state active</verify>
  <done>List command displays projects in requested format with optional state filtering</done>
</task>

</tasks>

<verification>
1. Running `activate list` shows a formatted table of projects
2. Running `activate list --format json` outputs valid JSON
3. Running `activate list --format tsv` outputs tab-separated values
4. Running `activate list --state inactive` filters by state
5. Empty database shows "No projects tracked" message
</verification>

<success_criteria>
- List command works with all three output formats
- Table format uses colors and relative timestamps
- JSON format is valid and pretty-printed
- TSV format is suitable for Unix pipeline processing
- State filtering works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-crud/01-04-SUMMARY.md`
</output>