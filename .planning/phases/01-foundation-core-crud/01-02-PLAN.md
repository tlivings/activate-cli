---
phase: 01-foundation-core-crud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/database/mod.rs, src/database/schema.rs, src/database/models.rs, src/database/operations.rs]
autonomous: true

must_haves:
  truths:
    - "Database file is created at ~/.config/activate/db.sqlite"
    - "Database has projects table with all required columns"
    - "Database supports migrations for future schema changes"
  artifacts:
    - path: "src/database/mod.rs"
      provides: "Database connection management"
      exports: ["open_database", "Database"]
    - path: "src/database/schema.rs"
      provides: "Schema creation and migration system"
      exports: ["migrate", "MIGRATIONS"]
    - path: "src/database/models.rs"
      provides: "Project model and types"
      exports: ["Project", "ProjectState"]
  key_links:
    - from: "src/database/operations.rs"
      to: "rusqlite"
      via: "prepared statements"
      pattern: "conn.prepare.*INSERT.*projects"
---

<objective>
Implement SQLite database layer with schema, migrations, and CRUD operation foundations.

Purpose: Create a robust data persistence layer that prevents path duplicates and supports future schema evolution.
Output: Database module with migrations, models, and connection management ready for command integration.
</objective>

<execution_context>
@/Users/tlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-crud/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database schema and migration system</name>
  <files>src/database/mod.rs, src/database/schema.rs</files>
  <action>
    Set up database connection and migration infrastructure.

    In src/database/mod.rs:
    - Create open_database() function using directories crate for path
    - Ensure parent directory exists before opening
    - Configure SQLite pragmas: WAL mode, foreign keys ON, busy timeout 5000ms
    - Call migrate() on connection before returning
    - Create Database struct wrapping Connection for easier passing
    - Export necessary types and functions

    In src/database/schema.rs:
    - Define MIGRATIONS constant array with version 1 schema
    - Create projects table with columns:
      * id INTEGER PRIMARY KEY AUTOINCREMENT
      * name TEXT NOT NULL UNIQUE COLLATE NOCASE (case-insensitive uniqueness)
      * path TEXT NOT NULL UNIQUE (canonical paths only)
      * state TEXT NOT NULL CHECK(state IN ('active', 'inactive', 'archived'))
      * last_touched INTEGER NOT NULL (Unix timestamp)
      * git_origin TEXT (nullable for Phase 3)
      * created_at INTEGER NOT NULL DEFAULT (unixepoch())
      * updated_at INTEGER NOT NULL DEFAULT (unixepoch())
    - Add indexes on state and last_touched for query performance
    - Create schema_version table for tracking migrations
    - Implement migrate() function that runs pending migrations
    - Implement get_schema_version() and set_schema_version() helpers

    Use rusqlite's execute_batch for migration SQL.
    Handle errors with anyhow context messages.
  </action>
  <verify>cargo test in database module, check that db.sqlite file is created</verify>
  <done>Database opens successfully with migrations applied, schema_version table shows version 1</done>
</task>

<task type="auto">
  <name>Task 2: Define models and implement CRUD operations</name>
  <files>src/database/models.rs, src/database/operations.rs</files>
  <action>
    Create data models and basic CRUD operations for projects.

    In src/database/models.rs:
    - Define ProjectState enum (Active, Inactive, Archived)
    - Implement Display and FromStr for database serialization
    - Define Project struct with all fields from schema
    - Implement from_row() method for rusqlite Row mapping
    - Use chrono::DateTime<Utc> for timestamps
    - Derive Debug, Clone, Serialize for output formatting

    In src/database/operations.rs:
    - Implement add_project(conn, name, path):
      * Canonicalize path using utils function
      * Check for duplicate path before insert
      * Use prepared statement with params!
      * Set initial state as 'inactive'
      * Set last_touched to current timestamp
      * Return project ID on success

    - Implement remove_project(conn, name):
      * Delete by name (case-insensitive match)
      * Return bool indicating if row was deleted

    - Implement list_projects(conn, state_filter):
      * Optional state filter parameter
      * Order by last_touched DESC
      * Use query_map to build Vec<Project>
      * Handle empty results gracefully

    - Implement get_project_by_name(conn, name):
      * Case-insensitive lookup
      * Return Option<Project>

    All functions return anyhow::Result with proper context.
    Use rusqlite params! macro for all queries.
  </action>
  <verify>Create unit tests for add/remove/list operations</verify>
  <done>CRUD operations work with proper error handling, no SQL injection possible</done>
</task>

</tasks>

<verification>
1. Database file created at ~/.config/activate/db.sqlite
2. Running migrations creates projects table with correct schema
3. Can add, remove, and list projects through operations module
4. Path canonicalization prevents duplicate entries
</verification>

<success_criteria>
- Database module compiles without warnings
- Migrations run automatically on first connection
- CRUD operations have comprehensive error handling
- Schema enforces data integrity constraints
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-crud/01-02-SUMMARY.md`
</output>