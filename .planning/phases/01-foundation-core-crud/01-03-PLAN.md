---
phase: 01-foundation-core-crud
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: [src/commands/mod.rs, src/commands/add.rs, src/commands/remove.rs, src/main.rs]
autonomous: true

must_haves:
  truths:
    - "User can add a project by path with activate add /path/to/project"
    - "User can remove a project by name with activate remove project-name"
    - "Duplicate paths are rejected with helpful error message"
  artifacts:
    - path: "src/commands/add.rs"
      provides: "Add command implementation"
      exports: ["execute_add"]
    - path: "src/commands/remove.rs"
      provides: "Remove command implementation"
      exports: ["execute_remove"]
  key_links:
    - from: "src/commands/add.rs"
      to: "src/database/operations.rs"
      via: "add_project function"
      pattern: "operations::add_project"
---

<objective>
Implement add and remove commands to manage projects in the database.

Purpose: Enable users to track and untrack projects with proper path handling and error messages.
Output: Working add/remove commands that interact with the database layer.
</objective>

<execution_context>
@/Users/tlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-crud/01-01-SUMMARY.md
@.planning/phases/01-foundation-core-crud/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement add and remove commands</name>
  <files>src/commands/mod.rs, src/commands/add.rs, src/commands/remove.rs</files>
  <action>
    Create command implementations that use the database operations.

    In src/commands/mod.rs:
    - Create module structure exporting add and remove modules
    - Define a CommandResult type alias for consistency

    In src/commands/add.rs:
    - Create execute_add(db, path, name_override) function
    - If path doesn't exist, return error with helpful message
    - Canonicalize the path using utils::paths::canonicalize_project_path
    - Extract project name from directory name or use override
    - Validate name (no slashes, reasonable length)
    - Call database::operations::add_project
    - Handle duplicate path error with "Project already tracked at this path"
    - Handle duplicate name error with "Project with this name already exists"
    - Print success message with colored output: "✓ Added project 'name' at path"

    In src/commands/remove.rs:
    - Create execute_remove(db, name) function
    - Call database::operations::remove_project
    - If project not found, list similar names as suggestions
    - Print confirmation: "✓ Removed project 'name'"
    - Return error if project doesn't exist

    Use anyhow for error handling with context messages.
    Use colored crate for success/error formatting.
  </action>
  <verify>cargo run -- add . and cargo run -- remove test-project work correctly</verify>
  <done>Add command stores projects in database, remove command deletes them</done>
</task>

<task type="auto">
  <name>Task 2: Wire commands into main CLI</name>
  <files>src/main.rs</files>
  <action>
    Connect the CLI parser to command implementations.

    Update src/main.rs:
    - Import commands module
    - Import database module
    - In main():
      * Parse CLI arguments
      * Load configuration
      * Open database connection
      * Match on parsed commands:
        - Commands::Add { path, name } => commands::add::execute_add(&db, &path, name)
        - Commands::Remove { name } => commands::remove::execute_remove(&db, &name)
        - Commands::List { ... } => todo!("List command in next plan")
      * Handle command results with proper exit codes
      * Print errors with red color and context chain

    Error handling:
    - Database errors: "Failed to open database: {reason}"
    - Command errors: Display full error chain
    - Exit with code 1 on error, 0 on success

    Make sure all paths through main return anyhow::Result.
  </action>
  <verify>cargo run -- add /tmp/test && cargo run -- remove test</verify>
  <done>CLI dispatches add/remove commands to implementations, errors shown clearly</done>
</task>

</tasks>

<verification>
1. Running `activate add /path/to/project` adds it to database
2. Running `activate add /same/path` shows duplicate error
3. Running `activate remove project-name` removes it
4. Running `activate remove nonexistent` shows not found error
</verification>

<success_criteria>
- Add command validates paths and handles duplicates gracefully
- Remove command provides helpful feedback when project not found
- Success messages use color and checkmarks for clarity
- All errors include context about what went wrong
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-crud/01-03-SUMMARY.md`
</output>